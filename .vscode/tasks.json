{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build asm",
            "type": "shell",
            "command": "make -f Makefile",
            "presentation": {
                "reveal": "never"
            },
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}